{
  "name": "electorn-container",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node scripts/start.js",
    "build": "node scripts/build.js",
    "dev": "node scripts/dev.js && electron --inspect=5858 ./src/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electorn": "0.0.1-security"
  },
  "devDependencies": {
    "inquirer": "^8.2.0"
  }
}
